package de.hawhh.informatik.sml.kino.fachwerte;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertNotNull;
import static org.junit.Assert.assertTrue;

import org.junit.Test;

public class GeldBetragTestDummy
{
	/*
	    @Test
	    public void testeAdd()
	    {
	    		GeldBetrag geldBetrag = GeldBetrag.get(100);
	    		GeldBetrag geldBetrag2 = GeldBetrag.get(200);
	    		GeldBetrag geldBetrag3 = GeldBetrag.get(300);
	    		
	    		geldBetrag.add(geldBetrag2);
	    		assertEquals(3, geldBetrag.getEuro());
	    		
	    		geldBetrag.add(geldBetrag3);
	    		
	    		assertEquals(6, geldBetrag.getEuro());
	    		
	    }

	    @Test
	    public void testeGet()
	    {
	    		GeldBetrag geldBetrag = GeldBetrag.get(100);
	    		GeldBetrag geldBetrag2 = GeldBetrag.get(250);
	    		GeldBetrag geldBetrag3 = GeldBetrag.get(4999);
	    		
	    		assertEquals(2, geldBetrag2.getEuro());
	    		assertEquals(50, geldBetrag2.getCent());
	    		assertEquals(0, geldBetrag.getCent());
	    		assertEquals(1, geldBetrag.getEuro());
	    		assertEquals(49, geldBetrag3.getEuro());
	    		assertEquals(99, geldBetrag3.getCent());
	    }
	
*/
}
